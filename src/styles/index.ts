import { SxStyleProp } from 'theme-ui';

interface IStyles {
  content: SxStyleProp;
}

const styles: IStyles = {
  content: {
    width: '100%',
    maxWidth: '900px',
    margin: 'auto',
  },
};

export default styles;
