export interface Genre {
  id: number;
  name: string;
}

export interface Movie {
  id: number;
  overview: string;
  popularity: number;
  poster_path: string;
  release_date: string;
  title: string;
}
